name: Colored Port Canonicalization Profile

on:
  push:
    branches:
      - 'research/weighted-colored-port-generation'
    paths:
      - '.github/workflows/ColoredPortCanonicalizationProfile.yml'
      - 'src/colored_port_generation.jl'
      - 'research/colored_port_canonicalization_profile.jl'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  profile:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: julia-actions/setup-julia@v3
        with:
          version: 'lts'
          show-versioninfo: true
      - uses: julia-actions/cache@v3
      - name: Instantiate benchmark environment
        run: |
          julia --project=benchmarks -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'
      - name: Profile canonicalization kernel
        run: |
          julia --project=benchmarks research/colored_port_canonicalization_profile.jl | tee colored-port-canonicalization-profile.txt
      - uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: colored-port-canonicalization-profile
          path: colored-port-canonicalization-profile.txt
